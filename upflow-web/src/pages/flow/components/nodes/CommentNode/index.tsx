import React from 'react';
import { NodeProps } from '@xyflow/react';

const CommentNode: React.FC<NodeProps> = ({ data, id }) => {
  return (
    <div>
      待实现
    </div>
  );
};

export default CommentNode;